create_task:
  fields:
    title:
      required: true
      selector:
        text:
    description:
      required: false
      selector:
        text:
          multiline: true

complete_task:
  fields:
    task_id:
      required: false
      selector:
        text:
    title:
      required: false
      selector:
        text:

sync_todo:

search_tasks:
  fields:
    query:
      required: true
      selector:
        text:
    status:
      required: false
      selector:
        select:
          options:
            - todo
            - in_progress
            - done

get_overdue_tasks:

notify_overdue:
