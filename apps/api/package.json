{
  "name": "@meitheal/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir=dist",
    "check": "tsc --noEmit && pnpm run check:size",
    "check:size": "node ./scripts/check-worker-size.mjs"
  },
  "dependencies": {
    "@meitheal/integration-core": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260305.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.69.0"
  }
}
