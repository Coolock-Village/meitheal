# Plan 04-01: D1 Database Adapter & Wrangler Config

**Wave:** 1
**Scope:** D1 adapter, wrangler.toml, migrations setup, environment config

## Tasks

### T-401: D1 Database Adapter
- Create `packages/integration-core/src/d1-adapter.ts`
- Wraps D1 binding with typed query interface
- Compatible API with existing SQLite usage patterns
- Handles D1-specific error codes and retry logic

### T-402: Wrangler Configuration
- Create `apps/api/wrangler.toml`
- D1 database binding
- Environment variables: `MEITHEAL_RUNTIME`, `MEITHEAL_VIKUNJA_API_TOKEN`
- Compatibility date, node_compat flags

### T-403: Shared Migration Infrastructure
- Create `apps/api/migrations/` directory
- Port existing SQLite migrations to D1 format
- Migration runner for D1 (via wrangler d1 migrations apply)

### T-404: Runtime Detection
- Create `packages/integration-core/src/runtime.ts`
- Detects HA vs Cloudflare runtime from environment
- Provides typed config accessor for runtime-specific settings

### T-405: Tests
- D1 adapter query/error handling (mocked D1 binding)
- Runtime detection for both environments
- Wrangler config validates

**Total:** 5 tasks
