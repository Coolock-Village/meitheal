# Plan 04-02: Worker API Routes & Integration

**Wave:** 2 (depends on Plan 01)
**Scope:** Expand worker.ts with compat routes, rate limiting, webhook dispatch

## Tasks

### T-411: Worker Router
- Expand `apps/api/src/worker.ts` with path-based router
- Route structure mirrors Astro `/api/v1/*` compat routes
- GET/POST/PUT/DELETE for tasks
- Bearer token auth middleware

### T-412: Compat Task Routes
- POST `/api/v1/tasks` — create task
- GET `/api/v1/tasks` — list tasks
- GET `/api/v1/tasks/:id` — get task
- PUT `/api/v1/tasks/:id` — update task
- DELETE `/api/v1/tasks/:id` — delete task
- Delegate to shared domain packages for business logic

### T-413: Rate Limiter Integration
- Reuse `RateLimiter` from integration-core
- Apply to all `/api/v1/*` routes
- X-Forwarded-For extraction via CF-Connecting-IP header

### T-414: KCS Documentation
- Cloud deployment guide: `docs/kcs/cloudflare-deployment-guide.md`
- Dual-runtime architecture notes
- D1 migration guide
- Environment configuration

### T-415: Tests
- Worker router dispatches to correct handlers
- Auth middleware rejects unauthorized requests
- Rate limiter integrates with Workers headers

**Total:** 5 tasks
