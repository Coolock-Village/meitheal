---
phase: 06-kanban-overhaul
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03, 04]
files_modified:
  - apps/web/src/pages/kanban.astro
  - apps/web/src/styles/_kanban.css
autonomous: true

must_haves:
  truths:
    - "Static inline styles migrated to CSS classes — count drops from 35 to ≤10"
    - "Only dynamic render-time styles remain inline (type dot colors, card border)"
    - "Lane management panel uses CSS classes not inline styles"
    - "Card metadata spans use semantic CSS not inline color"
    - "Quick-add form uses CSS classes for sizing/spacing"
  artifacts:
    - path: "apps/web/src/styles/_kanban.css"
      provides: "New CSS classes for toolbar, lane mgmt, quick-add, card metadata"
    - path: "apps/web/src/pages/kanban.astro"
      provides: "HTML/JS with inline styles replaced by class references"
---

<objective>
Migrate 18+ static inline styles in kanban.astro to CSS classes in _kanban.css.
Reduce inline style count from 35 to ≤10 (only dynamic values remain).

Purpose: Code quality and maintainability — inline styles are harder to override, debug, and maintain.
Output: Cleaner HTML with centralized styling.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS classes for toolbar, lane mgmt, quick-add, metadata</name>
  <files>apps/web/src/styles/_kanban.css</files>
  <action>
    Add the following CSS classes to _kanban.css (before the closing `}`):

    ```css
    /* --- Toolbar inline style migrations --- */
    .kanban-actions-slot {
      display: flex;
      gap: 8px;
      align-items: center;
    }

    .kanban-type-toolbar {
      padding: 8px 0;
    }

    .toolbar-label {
      margin-right: 4px;
    }

    /* --- Lane management panel --- */
    .lane-mgmt-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 12px;
    }

    .lane-mgmt-close {
      font-size: 18px;
      padding: 4px 8px;
    }

    .lane-mgmt-add-row {
      display: flex;
      gap: 8px;
      margin-top: 8px;
    }

    .lane-mgmt-add-input {
      flex: 1;
      font-size: 13px;
    }

    .lane-mgmt-add-btn {
      font-size: 13px;
    }

    /* --- Lane item rows (JS template) --- */
    .lane-item {
      display: flex;
      align-items: center;
      gap: 8px;
      padding: 6px 0;
      border-bottom: 1px solid var(--border);
    }

    .lane-drag-handle {
      cursor: grab;
      color: var(--text-muted);
      font-size: 12px;
    }

    .lane-item-label {
      flex: 1;
      font-size: 13px;
    }

    .lane-wip-label {
      font-size: 11px;
      color: var(--text-muted);
      display: flex;
      align-items: center;
      gap: 4px;
    }

    .lane-wip-input {
      width: 40px;
      font-size: 11px;
      padding: 2px 4px;
      border: 1px solid var(--border);
      border-radius: 4px;
      background: var(--bg-card);
      color: var(--text-primary);
    }

    .lane-action-btn {
      background: none;
      border: none;
      cursor: pointer;
      font-size: 14px;
    }

    .lane-action-btn.edit { color: var(--text-muted); }
    .lane-action-btn.remove { color: var(--danger); }

    .lane-builtin-label {
      font-size: 11px;
      color: var(--text-muted);
    }

    /* --- Quick-add form --- */
    .quick-add-input {
      flex: 1;
      font-size: 13px;
      padding: 6px 10px;
    }

    .quick-add-btn {
      font-size: 13px;
      padding: 6px 12px;
    }

    /* --- Card metadata text color --- */
    .kanban-card-due {
      color: var(--text-primary);
    }
    ```
  </action>
  <verify>
    - grep for new class names in _kanban.css
    - `npm run build` passes
  </verify>
  <done>
    All CSS classes for inline style migration are in _kanban.css.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace inline styles with CSS classes in kanban.astro</name>
  <files>apps/web/src/pages/kanban.astro</files>
  <action>
    Replace inline styles with class references:

    1. Line 167: `style="display: flex; gap: 8px; align-items: center;"` → `class="kanban-actions-slot"`
    2. Line 188: `style="padding: 8px 0;"` → add `kanban-type-toolbar` to existing class
    3. Line 257: `style="margin-right: 4px;"` → add `toolbar-label` to existing class
    4. Line 271: `style="margin-right: 4px;"` → add `toolbar-label` to existing class
    5. Line 290: `style="display: flex; justify-content:..."` → `class="lane-mgmt-header"`
    6. Line 298: `style="font-size: 18px; padding: 4px 8px;"` → add `lane-mgmt-close` to existing class
    7. Line 302: `style="display: flex; gap: 8px; margin-top: 8px;"` → `class="lane-mgmt-add-row"`
    8. Line 308: `style="flex: 1; font-size: 13px;"` → add `lane-mgmt-add-input` to existing class
    9. Line 314: `style="font-size: 13px;"` → add `lane-mgmt-add-btn` to existing class
    10. Line 531,551,578: `style="color:var(--text-primary);"` → REMOVE (handled by CSS class)
    11. Line 1041: `style="flex:1;font-size:13px;padding:6px 10px;"` → add `quick-add-input` to existing class
    12. Line 1042: `style="font-size:13px;padding:6px 12px;"` → add `quick-add-btn` to existing class

    For JS template literal inline styles (lines 1139-1151):
    13. Line 1139: use `class="lane-item"` (remove inline style)
    14. Line 1140: use `class="lane-drag-handle"` (remove inline style)
    15. Line 1141: use `class="lane-item-label"` (replace inline style)
    16. Line 1142: use `class="lane-wip-label"` (replace label inline style)
    17. Line 1143: add `class="lane-wip-input"` (remove style attribute, keep rest)
    18. Line 1148: use `class="lane-action-btn edit"` (replace inline style)
    19. Line 1149: use `class="lane-action-btn remove"` (replace inline style)
    20. Line 1151: use `class="lane-builtin-label"` (replace inline style)
  </action>
  <verify>
    - `grep -c 'style=' apps/web/src/pages/kanban.astro` → expect ≤10
    - `npm run build` passes
  </verify>
  <done>
    Inline style count drops from 35 to ≤10. Only dynamic styles remain.
  </done>
</task>

</tasks>

<verification>
1. `grep -c 'style=' apps/web/src/pages/kanban.astro` → ≤10
2. `npm run build` passes
3. Tests: 285 pass, 0 regressions
</verification>

<success_criteria>
- Inline styles ≤10 (from 35)
- Only dynamic values remain inline
- Build passes
- Tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-kanban-overhaul/06-05-SUMMARY.md`
</output>
