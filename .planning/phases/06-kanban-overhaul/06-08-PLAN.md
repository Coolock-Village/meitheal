# Phase 6 Plan 08: Card Inline Quick-Edit

## Objective
Double-click card title to edit in-place (Teamhood parity).

## Tasks

### Task 1: Wrap title text in a span
- Line 469: `{String(task.title)}` → `<span class="kanban-title-text" data-task-id={String(task.id)}>{String(task.title)}</span>`

### Task 2: Add JS dblclick handler
- Double-click on `.kanban-title-text` → `contentEditable = "true"`, select text
- Enter/blur → PUT `/api/tasks/{id}` with `{ title: newText }`
- Escape → revert to original text
- Prevent drag while editing

### Task 3: CSS for edit mode
- `.kanban-title-text[contenteditable]` — subtle border, background change

## Verification
```bash
npm run build && pnpm --filter @meitheal/tests test
```
