---
phase: "07"
plan: "02"
subsystem: shared-filter-toolbar
status: IN_PROGRESS
started: 2026-03-14
---

# Phase 7 Plan 02: Shared FilterToolbar Component

## Goal

Extract a unified `FilterToolbar.astro` shared component from the table and kanban inline filter HTML, eliminating ~200 lines of duplication. The component will emit `meitheal:filter-change` CustomEvents, allowing each view's controller to react consistently.

## Tasks

### T1: Create FilterToolbar.astro
- [/] Props: `view`, `showSearch`, `showGroupBy`, `showStatusFilter`, `showPriorityFilter`, `showRICEFilter`, `showUserFilter`
- [/] Type toggles (Epic/Story/Task) — shared between both views
- [/] Board selector (populated client-side)
- [/] Group-by selector (table: full options, kanban: type/board/priority)
- [/] Status/Priority/RICE/User select filters (table-only by default)
- [/] LabelFilterBar integration
- [/] Clear all button
- [/] `meitheal:filter-change` CustomEvent emission

### T2: Swap into table.astro
- [ ] Replace inline filter bar HTML (lines 113-207) with `<FilterToolbar>`
- [ ] Update table-controller.ts to listen for `meitheal:filter-change` events

### T3: Swap into kanban.astro
- [ ] Replace inline type-toolbar HTML (lines 185-292) with `<FilterToolbar>`
- [ ] Update kanban inline script to listen for `meitheal:filter-change` events

### T4: CSS consolidation
- [ ] Move shared filter/toolbar styles from `_table.css` and kanban `<style>` into shared `_filter-toolbar.css`

### T5: Verification
- [ ] TypeScript: `pnpm check` passes
- [ ] Tests: `pnpm test` — zero regressions
- [ ] Browser: table filters work identically to Phase 1
- [ ] Browser: kanban filters work identically to pre-change
