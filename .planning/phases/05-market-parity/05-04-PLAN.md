# Plan 05-04: Docs, CI, Governance

**Wave:** 4
**Scope:** OpenAPI spec, API reference, CI wrangler deploy, GDPR endpoint, README updates, D1 down migration

## Tasks

### T-531: OpenAPI Spec (Audit #8)
- Generate `apps/api/openapi.yaml` describing all Worker routes
- Include auth scheme, request/response schemas, error codes
- Reference from AI_POLICY.md

### T-532: Worker API Reference (Audit #42)
- Write `docs/kcs/worker-api-reference.md`
- Document all routes, auth, rate limiting, error codes
- Include curl examples for each endpoint

### T-533: GDPR Data Deletion Endpoint (Audit #33)
- `DELETE /api/v1/user/data` — deletes all user data
- Requires auth, returns confirmation
- Emits `user.data.deleted` domain event

### T-534: CI Wrangler Deploy Step (Audit #29)
- Document `wrangler deploy` CI step in deployment guide
- Add `deploy` script to `apps/api/package.json`
- Update README with dual-runtime dev instructions (#44)

### T-535: D1 Down Migration (Audit #11)
- Add `0001_create_tasks.down.sql` to `apps/api/migrations/`
- `DROP TABLE IF EXISTS tasks`

### T-536: Tests
- OpenAPI spec valid YAML
- GDPR endpoint returns 200 on auth'd delete
- Down migration SQL parseable

**Total:** 6 tasks
