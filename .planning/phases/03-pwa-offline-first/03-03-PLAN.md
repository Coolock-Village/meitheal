# Plan 03-03: Sync Status UI & KCS Docs

**Wave:** 2 (depends on Plan 01 + 02)
**Scope:** Sync indicator component, offline-first KCS docs, operations runbook update

## Tasks

### T-321: Sync Status Component
- Create `apps/web/src/components/SyncStatus.astro` (or .ts for client island)
- States: synced ✓ / syncing ↻ / pending ● / offline ⊘
- Persistent badge in app shell (bottom-left or status bar area)
- Reactive to: online/offline events, sync engine state, queue depth
- Accessible: aria-live region for screen readers

### T-322: Online/Offline Detection
- Create `apps/web/src/domains/offline/connectivity.ts`
- navigator.onLine + periodic fetch ping to /api/health
- Debounce: 2s before transitioning to offline (avoid flicker)
- Emit events for UI components and sync engine

### T-323: KCS Documentation
- PWA setup guide: `docs/kcs/pwa-offline-guide.md`
  - How offline mode works
  - What data is available offline
  - Sync conflict resolution behavior
  - Storage limits and warnings
  - Troubleshooting: clearing cache, forcing sync
- Update `docs/kcs/operations-runbook.md` with PWA operational notes

### T-324: Performance Verification
- Lighthouse PWA audit (target: all checks pass)
- Precache budget verification (≤ 1MB)
- IndexedDB storage measurement
- Offline task create/read latency (< 50ms target)

### T-325: Tests
- SyncStatus component renders correct state for each mode
- Connectivity detector fires events on online/offline
- Offline indicator accessible (aria-live)

**Total:** 5 tasks
