---
reconstructed_from:
  - 3914d86
  - 03223c0
  - c2d1750
evidence_sources:
  - .planning/persona-loops/phase-1/iteration-01/04-implementation-log.md
  - .planning/persona-loops/phase-1/iteration-02/04-implementation-log.md
  - .planning/persona-loops/phase-1/iteration-03/04-implementation-log.md
  - .planning/persona-loops/phase-1/iteration-04/04-implementation-log.md
---

# Plan 01-01: Foundation and Persistent Vertical Slice (Reconstructed)

## Objective

Build the initial production baseline for Meitheal and deliver one HA-linked, persistent vertical slice end-to-end.

## Reconstructed Workstreams

### WS-101: Monorepo and governance baseline

- Create Astro-first monorepo structure and package boundaries.
- Add required governance artifacts (`README.md`, `AGENTS.md`, `SKILL.md`, `WEBMCP.md`, `.zeroclaw/soul.md`).
- Add CI/governance baseline tests and required file enforcement.

### WS-102: Core domain and runtime baseline

- Create bounded contexts (`domain-auth`, `domain-tasks`, `domain-strategy`, `domain-observability`, `integration-core`).
- Add web runtime (`apps/web`) with content schemas and middleware.
- Add HA add-on package (`addons/meitheal-hub`) with run script and observability pipeline baseline.

### WS-103: Persistent vertical slice

- Replace memory confirmation path with persistence-backed flow.
- Implement task creation domain service with framework payload handling.
- Implement calendar sync adapter and persistent confirmation endpoint.
- Add idempotency and replay-safe behavior in persistence/service layer.

### WS-104: Verification and release plumbing

- Add/expand tests for persistence, sync, redaction, and governance.
- Add PR and branch-protection flow for `Coolock-Village/meitheal`.
- Ensure `pnpm check` and workspace tests run in local + CI pathways.
