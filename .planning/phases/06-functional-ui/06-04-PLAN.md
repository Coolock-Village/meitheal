# Phase 6 — Wave 4: Labels + Projects + HA Surfaces + Polish

## Scope
Label management, project switching, HA integration UI, framework scoring display, and final polish.

## Tasks

### T-631: Label Management
- **File:** `apps/web/src/pages/labels.astro`
- CRUD for labels with name + color picker
- Color palette: 12 preset colors + custom hex
- Labels displayed as colored badges on all task views
- Label filter integration
- Uses existing `/api/v1/labels` API

### T-632: Project Sidebar
- Project list in sidebar with counts
- Default "Inbox" project for unassigned tasks
- Project create/rename/delete
- Project filter applied to all views
- Maps to Vikunja `/api/v1/projects` compatibility

### T-633: HA Integration Panel
- **File:** `apps/web/src/pages/settings.astro`
- HA connection status (token present, reachable)
- Calendar entity selector
- Calendar sync status per task (badge: ✅ synced, ⏳ pending, ❌ failed)
- Manual "Sync Now" button
- Grocy connection status (if configured)

### T-634: Framework Scoring Display
- RICE score column in table view
- RICE badge on task cards (reach × impact × confidence / effort)
- Scoring input fields in task edit modal
- Sort by RICE score in list and table views
- Visual score indicator (low/medium/high with color)

### T-635: Keyboard Shortcuts + Polish
- `n` → new task, `/` → search, `k` → kanban, `l` → list, `t` → table
- Keyboard shortcut help modal (`?`)
- Loading skeletons on page transitions
- 404 page with Meitheal branding
- Favicon (☘ shamrock)

### T-636: Offline Indicators
- "Offline" banner when disconnected
- Pending sync count badge in sidebar
- Sync status toast on reconnection
- Works with existing offline-store.ts + sync-engine.ts

## Verification
- All views render correctly with real data
- Labels display on task cards across views
- Framework scoring calculates and displays
- Keyboard shortcuts work
- Container build succeeds
- Screenshots captured for README
