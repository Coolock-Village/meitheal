---
wave: 2
depends_on: ["20-01: Empty States & Loading Skeletons"]
files_modified: []
autonomous: true
---

# 20-02: API Hardening & Input Sanitization

**Goal:** Secure all API endpoints with strong validation and rate limiting infrastructure.

## Context

Production readiness requires robust API security beyond just session auth. Check existing API endpoints (`/api/tasks`, `/api/boards`, `/api/health`) for missing validation logic and enhance the Astro middleware to validate incoming types and reject malformed data securely.

## Tasks

```xml
<tasks>
  <task id="20-02-01" type="modify">
    <file>apps/web/src/pages/api/tasks/index.ts</file>
    <description>Add generic input sanitization or validation checks where data enters the system from client.</description>
    <must_haves>
      <must_have>API must handle missing fields cleanly.</must_have>
    </must_haves>
  </task>
</tasks>
```

## Verification

1. Trigger validation failures from client to confirm proper 400 Bad Request error codes.
