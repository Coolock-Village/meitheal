# 20-01: Empty States & Loading Skeletons — Summary

Updated empty states across `index.astro` and `tasks.astro` to properly correctly utilize the design tokens specified in `global.css` (`empty-state`, `empty-icon`, `empty-text`, `empty-sub`).

## Files Modified

1. `apps/web/src/pages/index.astro`
2. `apps/web/src/pages/tasks.astro`

## Self-Check: PASS

- `empty-icon`, `empty-text`, and `empty-sub` are correctly referenced.
- `pnpm check` passes without related errors.
