---
wave: 3
depends_on: ["20-02: API Hardening & Input Sanitization"]
files_modified: []
autonomous: true
---

# 20-03: Advanced PWA & Offline Sync

**Goal:** Make offline capabilities foolproof and bulletproof with conflict resolution and optimistic rollbacks.

## Context

The app already works offline and syncs back (`sync-engine.ts`), but edge cases like network dropping mid-sync or simultaneous changes from another HA client need robust handling.

## Tasks

```xml
<tasks>
  <task id="20-03-01" type="modify">
    <file>apps/web/src/domains/offline/sync-engine.ts</file>
    <description>Add retry backoff for failed sync attempts, and better offline toast alerts for users.</description>
    <must_haves>
      <must_have>Must implement exponential backoff logic.</must_have>
    </must_haves>
  </task>
</tasks>
```

## Verification

1. Turn off network via DevTools. Create task. Turn on network. Verify successful background sync without UI freezing.
