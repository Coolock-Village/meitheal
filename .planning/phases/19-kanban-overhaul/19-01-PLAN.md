---
wave: 1
depends_on: []
files_modified:
  - apps/web/src/pages/kanban.astro
  - apps/web/src/styles/global.css
autonomous: true
---

# Wave 1: P0 Critical Fixes

## Goal

Eliminate production-blocking issues: mobile layout, page reload on drop, inline task detail, dead UI elements, test data.

## Tasks

### T-1901: Mobile Column Stacking

Add CSS media query to stack kanban columns vertically at `<768px`. Currently columns use `display: flex` with no `flex-direction` change — add `@media (max-width: 768px) { .kanban-board { flex-direction: column; } }`. Also fix ✅ emoji wrapping at narrow widths.

**Files:** `global.css`
**Verify:** Browser at 375px shows stacked columns with no horizontal overflow.

### T-1902: Optimistic Drag-Drop (No Page Reload)

Replace `window.location.reload()` in `handleDrop()` with DOM manipulation:
1. Move the dragged card element to the target column's `.kanban-cards` container
2. Update column count badges in-place
3. Keep the API `PUT` call for persistence
4. Show toast without reload

**Files:** `kanban.astro` (script section)
**Verify:** Drag card between columns — card moves instantly, no page flash, count updates.

### T-1903: Inline Task Detail Panel

Wire card click to open the existing task detail panel (from Layout.astro Phase 18) instead of redirecting to `/tasks#edit-{id}`. The task detail panel already exists as `#task-detail-panel` in Layout — just need to:
1. Add click handler on `.kanban-card` that calls `openTaskDetail(id)` 
2. Remove the redirect in `.card-edit-btn` handler
3. Ensure panel opens with correct task data

**Files:** `kanban.astro` (script section)
**Verify:** Click card → side panel opens with task details, Kanban board stays visible.

### T-1904: Fix Lanes Button

The `#manage-lanes-btn` click handler exists but the `#lane-mgmt` panel is `display: none` and never toggled. Wire the button to toggle visibility of `#lane-mgmt` panel.

**Files:** `kanban.astro` (script section)
**Verify:** Click ⚙ Lanes → panel slides open, can add/remove lanes, close panel.

### T-1905: Clean Up Test Data Column

The 4th column `📌 nm,m,n` comes from custom lanes stored in the DB. Don't delete user data — but ensure custom lanes with empty/garbage titles show a "Custom Lane" fallback. Add input validation when creating lanes (min 1 char, max 50 chars, strip HTML).

**Files:** `kanban.astro` (lane rendering + lane creation handler)
**Verify:** Empty-titled lanes show "Custom Lane" label, new lanes require valid title.

## Verification Gate

- Mobile: columns stack at 375px (screenshot)
- Drag-drop: no reload, optimistic DOM update
- Card click: task detail panel opens inline
- Lanes button: toggles panel open/close
- All existing tests still pass
