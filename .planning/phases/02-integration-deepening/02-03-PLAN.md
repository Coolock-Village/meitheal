---
wave: 2
depends_on: [01]
files_modified:
  - packages/integration-core/src/grocy-adapter.ts
  - packages/integration-core/src/grocy-types.ts
  - packages/integration-core/src/index.ts
  - apps/web/src/domains/integrations/grocy-enrichment.ts
  - tests/e2e/grocy-adapter.spec.ts
autonomous: true
---

# Plan 03: Grocy Stock Check Adapter

<objective>
Add a read-only Grocy adapter for stock checks on task creation. Follows the same adapter pattern as HomeAssistantCalendarAdapter.
</objective>

<context>
Phase 2 CONTEXT.md decisions (locked):
- Read-only: checkStock(ingredients) and consumeItems(items)
- Config: MEITHEAL_GROCY_URL + MEITHEAL_GROCY_API_KEY env vars
- Follows CalendarIntegrationAdapter pattern from integration-core/
- Triggered optionally on task create with grocery framework fields
- Timeout/retry: 8s timeout, classifyError for retry (matches HA adapter)
</context>

<tasks>
<task id="T-221" priority="P1">
Create `packages/integration-core/src/grocy-types.ts`:
- `GrocyStockItem`: `{ product_id, name, amount, best_before_date }`
- `GrocyConsumeRequest`: `{ product_id, amount, transaction_type }`
- `GrocyStockCheckResult`: `{ ok: true, items: GrocyStockItem[] } | { ok: false, errorCode: string, message: string }`
</task>

<task id="T-222" priority="P1" depends="T-221">
Create `packages/integration-core/src/grocy-adapter.ts`:
- `GrocyStockAdapter` class following `HomeAssistantCalendarAdapter` structure
- Constructor: `{ baseUrl, apiKey, timeout? }`
- `checkStock(ingredients: string[]): Promise<GrocyStockCheckResult>` — GET `/api/stock`
- `consumeItems(items: GrocyConsumeRequest[]): Promise<void>` — POST `/api/stock/products/{id}/consume`
- AbortController + 8s timeout
- `classifyError(status)` for retry semantics (reuse existing function)
- Structured logging via domain-observability
</task>

<task id="T-223" priority="P1" depends="T-222">
Create `apps/web/src/domains/integrations/grocy-enrichment.ts`:
- Optional enrichment hook on task create
- Check if task has grocery-related framework fields
- If Grocy adapter configured: call `checkStock()` with field values
- Append stock status to task metadata
- Non-blocking: failure does not block task creation
</task>

<task id="T-224" priority="P1">
Write tests:
- `grocy-adapter.spec.ts`: successful stock check, HTTP error handling, timeout, retry on 429/5xx, empty results
- Mock Grocy API with local HTTP server pattern (matches HA harness)
</task>
</tasks>

<verification>
- [ ] `pnpm check` passes (0 errors)
- [ ] Grocy adapter tests pass
- [ ] Integration-core barrel export updated
- [ ] Task create still works without Grocy configured (non-blocking)
</verification>

<must_haves>
- Read-only adapter matching HA adapter pattern
- checkStock and consumeItems methods
- Non-blocking on task create (optional enrichment)
- Same timeout/retry semantics as HA calendar adapter
</must_haves>
