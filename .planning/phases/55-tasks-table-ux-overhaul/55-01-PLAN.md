---
phase: 55
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/task-queries.ts
  - apps/web/src/pages/tasks.astro
  - apps/web/src/pages/table.astro
  - apps/web/src/components/ui/RiceBadge.astro
  - apps/web/src/styles/global.css
  - apps/web/src/i18n/en.json
  - apps/web/src/i18n/ga.json
autonomous: true
---

# 55-01: Tasks & Table UX Overhaul — Core Improvements

## Goal

Fix P0-P2 findings from the 50-persona panel review. Deliver consistent filter/sort UX, accessible markup, clean CSS, and overdue indicators across both task view pages.

## Tasks

<task id="1" name="Create shared task-queries utility">
Extract duplicated data loading logic from both `tasks.astro` and `table.astro` into a shared `@lib/task-queries.ts` module.

**Includes:**
- SQL query for all task view fields
- RICE score calculation
- Custom field parsing
- Framework payload parsing
- Overdue detection (compare due_date against current date)
- Type-safe return interface

**Acceptance:** Both pages import `getTasksForView()` with zero duplicated SQL or RICE logic.
</task>

<task id="2" name="Add i18n keys for new UI elements">
Add missing translation keys to `en.json` and `ga.json`:
- `tasks.clear_filters` — "Clear All"
- `tasks.task_summary` — "{total} tasks · {active} active · {overdue} overdue"
- `tasks.overdue` — "Overdue"
- `tasks.due_prefix` — "Due"
- `table.empty_description` — i18n version of hardcoded string
- `table.priority_updated` / `table.status_updated` — use existing keys

**Acceptance:** No hardcoded English strings in either page template.
</task>

<task id="3" name="Fix RiceBadge thresholds">
Update `RiceBadge.astro` thresholds from ≥50/≥20 to ≥15/≥8 to match filter dropdown values.

**Acceptance:** Badge color matches what "High RICE" filter shows.
</task>

<task id="4" name="Add global CSS classes for new UI patterns">
Add to `global.css` (`@layer components`):
- `.filter-summary-bar` — task count summary layout
- `.task-actions` — hover-reveal container for list item action buttons
- `.overdue-date` — red highlight for past-due dates
- `.filter-clear-btn` — ghost button for filter reset
- Responsive table column hiding via `@media` at 768px and 600px breakpoints
- Filter item transition animation (opacity + max-height)

**Acceptance:** New classes defined; no inline style overrides needed in pages.
</task>

<task id="5" name="Refactor tasks.astro">
Major cleanup of the Tasks list page:
- Import `getTasksForView()` from shared module
- Remove inline `style=""` attributes — replace with CSS classes
- Remove duplicated `.view-switcher`/`.view-btn` scoped styles
- Add `role="button"` and `tabindex="0"` to `.task-item` divs
- Reveal action buttons (delete, duplicate, AI) on hover only
- Add task count summary bar above task list
- Add "Clear All" button to filter bar
- Add `.overdue-date` class to past-due dates
- Fix duplicate task handler: reload page instead of DOM clone
- Replace hardcoded "· Due" with i18n key
- Add shared filter state persistence key

**Acceptance:** Page is ~400 lines (from 599), zero inline styles, accessible markup.
</task>

<task id="6" name="Refactor table.astro">
Major cleanup of the Table page:
- Import `getTasksForView()` from shared module
- Add status and priority filter dropdowns (parity with tasks page)
- Add "Clear All" button to filter bar
- Add task count summary bar
- Fix numeric sort for priority/RICE columns
- Add `data-original` attribute to editable cells; revert on empty blur
- Add row click → task detail panel (delegate to Layout.astro openTaskDetail)
- Replace hardcoded hex priority colors with CSS custom properties
- Replace hardcoded description string with i18n key
- Remove duplicated scoped CSS already in global.css
- Add shared filter state persistence key
- Add responsive column hiding
- Add `.overdue-date` class to past-due dates

**Acceptance:** Filter parity with tasks page, numeric sort works, row click opens detail panel.
</task>

## Verification

```bash
cd /home/ryan/code/meitheal && pnpm run build 2>&1 | tail -5
```

- Build passes with zero errors
- Browser-verify both pages at `/tasks` and `/table`
- Confirm filter state persists when switching views

## must_haves

- [ ] Shared data loading — no duplicated SQL or RICE calculation
- [ ] Filter parity — both pages have identical filter controls
- [ ] Numeric sort — priority and RICE columns sort correctly
- [ ] Overdue indicators — past-due dates are visually highlighted
- [ ] Task count summary — user sees total/active/overdue at a glance
- [ ] Zero inline styles — all styling via CSS classes
- [ ] Accessible task items — keyboard navigable with proper ARIA
- [ ] i18n complete — no hardcoded English strings
