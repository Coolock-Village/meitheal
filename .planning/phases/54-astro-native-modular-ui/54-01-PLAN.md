---
stage: plan
objective: Align settings export, extract Astro components, and modularize navigation
autonomous: true
files_modified:
  - apps/web/src/pages/api/export/index.ts
  - apps/web/src/pages/api/import/index.ts
  - apps/web/src/pages/settings.astro
  - apps/web/src/components/layout/Sidebar.astro
---

# Plan 54-01: Modular UI & Settings Alignment

## Objective
Extract Astro-native components, make the sidebar navigation fully modular, and ensure settings configurations are seamlessly exported and imported.

## Tasks

1. **[ ] Task 1: Align Settings Export/Import**
   - Review `/api/export` and `/api/import` logic.
   - Guarantee that all rows in the `settings` table (including `sidebar_config`, `theme_config`, AI providers, Integrations) are extracted in the JSON backup.
   - During import, ensure settings are correctly upserted so a user's customized UI configuration is perfectly restored.

2. **[ ] Task 2: Componentify `settings.astro`**
   - The `settings.astro` file is massive (>2500 lines). Extract the major tabs (General, Integrations, Agents & AI, System & Danger) into separate Astro components in `src/components/settings/`.
   - Ensure the client-side JavaScript that controls tab switching and persisting data remains functional.

3. **[ ] Task 3: Modularize Navigation Systems**
   - Review `Sidebar.astro` and ensure it robustly handles custom navigation items.
   - Verify that the `sidebar_config` settings correctly toggle visibility and ordering.

## Key Files
- `apps/web/src/pages/api/export/index.ts`
- `apps/web/src/pages/api/import/index.ts`
- `apps/web/src/pages/settings.astro`
- `apps/web/src/components/layout/Sidebar.astro`

## Verification
- Run a data export and confirm `sidebar_config` is present in the `settings` payload.
- Verify `settings.astro` loads correctly and tabs switch without JS errors.
