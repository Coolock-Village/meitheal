# Phase 56 Plan 01: Settings IA Restructure

**Phase:** 56-settings-ia-overhaul
**Created:** 2026-03-03
**Status:** In progress (General tab done)

## Goal

Restructure Settings page IA per persona audit findings and user feedback.

## Tasks

### 1. General Tab — Sidebar First ✅ (already done)
- Remove HA Connection card from `SettingsGeneral.astro`
- Remove PWA Status card from `SettingsGeneral.astro`
- Order: Sidebar Config → Custom Fields → Strategic Lenses
- Remove HA/PWA CSS and JS from component

### 2. Integrations Tab — HA Connection Card
- Add HA Connection as first integration card in `SettingsIntegrations.astro`
- Use existing accordion pattern (same as Calendar, Todo, Grocy)
- Include: status indicator, version/timezone/mode info, Test Connection button
- Include: Advanced/Standalone mode (collapsed `<details>`)
- Move HA detection JS from old `SettingsGeneral` script block

### 3. Test Connection UX Fix
- Change text color from `var(--text-primary)` to `var(--text-secondary)`
- Cap component list at 5 items
- Add "Show all X components" expand toggle
- Add spacing between component items
- Use monospaced font for entity list at smaller size

### 4. System Tab — Compact PWA + Help
- Add PWA status as a single-line row in About card
- Format: dot indicator + label + badge (e.g., "PWA: ● Active" or "PWA: ● HTTP only")
- Clicking shows expandable HTTPS hint via `<details>` element
- Add Help & Support card before Danger Zone
- Include: project description, version, links (Issues, Discussions, Star)

### 5. JS/CSS Migration
- Move `initHAConnectionStatus()` function to `SettingsIntegrations.astro` script
- Move `initPwaStatus()` compact version to `SettingsSystem.astro` script
- Move HA-related CSS to `SettingsIntegrations.astro` style block
- Clean up any orphaned styles

### 6. Build + Verify + Commit

## Dependencies
- None — all self-contained within Settings components

## Files Modified
- `apps/web/src/components/settings/SettingsGeneral.astro` ✅
- `apps/web/src/components/settings/SettingsIntegrations.astro`
- `apps/web/src/components/settings/SettingsSystem.astro`
- `apps/web/src/pages/settings.astro` (JS handlers if needed)
