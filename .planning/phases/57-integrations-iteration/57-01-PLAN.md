# Phase 57-01: Integrations Page Iteration — Execution Plan

**Phase:** 57 — Integrations Page Iteration
**Wave:** 01 — Full implementation
**Status:** Ready for execution

## Objective

Enhance the Settings → Integrations tab with explainer popups for all integration cards, smarter auto-configuration, and WCAG AA-compliant theme readability fixes.

## Tasks

### T1: Theme Readability Fix (`global.css`)
**Files:** `apps/web/src/styles/global.css`
- Bump dark theme: `--text-muted` (#5e5e78→#6e6e8a), `--text-secondary` (#9b9bb4→#a8a8c0), `--border` (#2a2a42→#32324e), `--bg-secondary` (#1a1a2e→#1e1e34), `--bg-card` (#16213e→#1c2540), `--bg-primary` (#0f0f1a→#131320)
- Bump light theme: `--text-muted` (#8888a4→#7a7a96)
- Update all RGB channel vars to match
- **Autonomous:** true — pure CSS, no logic changes

### T2: Integration Explainer Modal (`SettingsIntegrations.astro`)
**Files:** `apps/web/src/components/settings/SettingsIntegrations.astro`
- Add native `<dialog>` modal component for integration explainers
- Add ℹ️ info button to each card header
- Write explainer content for all 5 integrations
- JS: init dialog open/close with `showModal()`/`close()`, Escape key, click outside
- A11y: `role="dialog"`, `aria-modal="true"`, focus trap, `aria-label`

### T3: Calendar Dropdown + CaldAV Section (`SettingsIntegrations.astro` + `settings.astro`)
**Files:** `apps/web/src/components/settings/SettingsIntegrations.astro`, `apps/web/src/pages/settings.astro`
- Replace `EntitySelector` with native `<select>` dropdown populated from detected HA entities
- Add "Custom entity…" option → reveals text input
- Move CaldAV into collapsible `<details>` section: "External Calendar (CalDAV)"
- Update `hydrateCalendarStatus()` to populate `<option>` elements

### T4: Todo Bridge Explainer (`SettingsIntegrations.astro`)
**Files:** `apps/web/src/components/settings/SettingsIntegrations.astro`
- Add callout at top of todo card body explaining bridge purpose
- Update subtitle text

### T5: Grocy Auto-Detection (`SettingsIntegrations.astro` + `settings.astro`)
**Files:** `apps/web/src/components/settings/SettingsIntegrations.astro`, `apps/web/src/pages/settings.astro`
- Add auto-detect callout panel (like calendar/todo pattern)
- Add `hydrateGrocyStatus()` — check HA addons list for Grocy slug
- Auto-fill URL from ingress when detected
- Show manual config only for standalone/override

### T6: n8n/Node-RED Mode Split (`SettingsIntegrations.astro` + `settings.astro`)
**Files:** `apps/web/src/components/settings/SettingsIntegrations.astro`, `apps/web/src/pages/settings.astro`
- Add mode toggle: HA Addon (auto) vs Standalone
- HA mode: show detection status, event checkboxes only
- Standalone mode: webhook URL + API key + secret fields
- Add `hydrateN8nStatus()` — detect Node-RED/n8n HA addons
- Save additional fields for standalone

### T7: Webhooks Differentiation (`SettingsIntegrations.astro`)
**Files:** `apps/web/src/components/settings/SettingsIntegrations.astro`
- Add info callout distinguishing webhooks from Agents & AI tab

### T8: Documentation Updates
**Files:** `.planning/codebase/INTEGRATIONS.md`, `.planning/codebase/STACK.md`, `.planning/STATE.md`
- Update integration docs with new features
- Update state with Phase 57 completion

## Success Criteria

- [ ] All 5 integration cards have explainer info button → modal works
- [ ] Calendar shows dropdown when HA entities detected
- [ ] CaldAV preserved in collapsible section
- [ ] Todo has bridge explainer callout
- [ ] Grocy auto-detects from HA
- [ ] n8n/Node-RED has HA addon vs standalone modes
- [ ] Webhooks has AI differentiation callout
- [ ] Dark theme text readable — `--text-muted` WCAG AA compliant
- [ ] Light theme text readable — verified
- [ ] `npm run build` passes
- [ ] Existing E2E tests pass
