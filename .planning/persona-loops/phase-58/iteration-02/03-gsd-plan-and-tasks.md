# GSD Tasks — Phase 58 Iteration 02

Synthesized from Panel 1 (Frontier) + Panel 2 (ADHD/Productivity). All accepted recommendations.

## Tasks

| # | Task | Source | GSD Command |
|---|------|--------|-------------|
| 1 | Rename duplicate `@keyframes skeleton-pulse` to `skeleton-fade` in `_feedback.css` | Platform Architect | execute |
| 2 | Extract SVG data URI to `--select-arrow` custom property in `_tokens.css` | OSS Integrations | execute |
| 3 | Add CSP audit comment to `_tokens.css` | Security Engineer | execute |
| 4 | Consolidate `.shortcut-modal-*` animations into `_modal.css`, `.content-area` animation into `_layout.css` | Product Architect | execute |
| 5 | Add class-map comments to `global.css` imports | Workflow Coach | execute |
| 6 | Update CI brace check to glob all CSS partials | Execution Coach | execute |
| 7 | Add domain boundary header comments to each partial | Knowledge Coach | execute |
